{
  "name": "business",
  "version": "1.0.0",
  "description": "a static grid css site",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist"
  },
  "author": "shina",
  "license": "MIT",
  "dependencies": {
    "gh-pages": "^2.0.1"
  }
}
